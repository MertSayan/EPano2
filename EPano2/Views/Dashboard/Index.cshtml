@model EPano2.Models.DashboardViewModel
@using Newtonsoft.Json
@{
    ViewData["Title"] = "ISUBU Bilgisayar Mühendisliği - Dijital Bilgi Panosu";
    Layout = null;
    var videoFilePathsJson = ViewBag.VideoFilePathsJson as string ?? "[]";
    var defaultVideoFilePath = ViewBag.DefaultVideoFilePath as string;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/css/dashboard.css" />
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
</head>
<body>
    <div class="dashboard-container">

        <!-- HEADER -->
        <header class="dashboard-header">
            <img class="header-logo-img" src="/image/logo2.jpg" alt="ISUBU Logo">
            <div class="header-title">ISPARTA UYGULAMALI BİLİMLER ÜNİVERSİTESİ</div>
            <img class="header-logo-img" src="/image/logo2.jpg" alt="ISUBU Logo">
        </header>

        <!-- MAIN -->
        <main class="dashboard-main">

            <!-- LEFT COLUMN -->
            <div class="left-column">

                <!-- Video -->
                <section class="video-section fade-in">
                    <h2 class="video-title">Videolar</h2>
                    <div class="video-player">
                        <video id="mainVideoPlayer"
                               class="video-element"
                               autoplay
                               muted
                               playsinline
                               style="width: 100%; height: 100%; object-fit: cover;">
                            Tarayıcınız video oynatmayı desteklemiyor.
                        </video>
                    </div>
                </section>

                <!-- Weather + Date -->
                <section class="info-wrapper">
                    <div class="weather-section fade-in">
                        <h3 class="weather-title">Günlük Hava Durumu</h3>
                        <div id="weatherToday" class="weather-today-content"></div>
                    </div>

                    <div class="datetime-section fade-in">
                        <h3 class="datetime-title">Tarih ve Saat</h3>
                        <div class="datetime-info" id="datetime-time">--:--:--</div>
                        <div class="datetime-date" id="datetime-date">--</div>
                    </div>
                </section>

            </div>

            <!-- RIGHT COLUMN -->
            <div class="right-column">

                <section class="announcements-section fade-in">
                    <h2 class="announcements-title">Duyurular - Haberler - Etkinlikler</h2>

                    <!-- SLIDER -->
                    <div class="announcements-carousel">
                        @* Render News (haber=true) separately - Start with News *@
                        @if (Model.News.Any())
                        {
                            foreach (var news in Model.News.OrderBy(a => a.ID))
                            {
                                <div class="announcement-slide" data-haber="true" data-etkinlik="false" style="background-image:url('@news.PosterImageUrl')">
                                    <div class="announcement-overlay">
                                        <h3 class="announcement-title">@news.Title</h3>
                                        <p class="announcement-content">@news.Content</p>
                                    </div>
                                </div>
                            }
                        }
                        
                        @* Render Announcements (haber=false) separately *@
                        @if (Model.Announcements.Any())
                        {
                            foreach (var ann in Model.Announcements.OrderBy(a => a.ID))
                            {
                                <div class="announcement-slide" data-haber="false" data-etkinlik="false" style="background-image:url('@ann.PosterImageUrl')">
                                    <div class="announcement-overlay">
                                        <h3 class="announcement-title">@ann.Title</h3>
                                        <p class="announcement-content">@ann.Content</p>
                                    </div>
                                </div>
                            }
                        }
                        
                        @* Render Events (etkinlik=true) separately *@
                        @if (Model.Etkinlikler.Any())
                        {
                            foreach (var etkinlik in Model.Etkinlikler.OrderBy(a => a.ID))
                            {
                                <div class="announcement-slide" data-haber="false" data-etkinlik="true" style="background-image:url('@etkinlik.PosterImageUrl')">
                                    <div class="announcement-overlay">
                                        <h3 class="announcement-title">@etkinlik.Title</h3>
                                        <p class="announcement-content">@etkinlik.Content</p>
                                    </div>
                                </div>
                            }
                        }
                        
                        @* Show empty state only if all lists are empty *@
                        @if (!Model.Announcements.Any() && !Model.News.Any() && !Model.Etkinlikler.Any())
                        {
                            <div class="announcement-slide active">
                                <div class="announcement-overlay">
                                    <h3 class="announcement-title">Duyuru Bulunamadı</h3>
                                </div>
                            </div>
                        }
                    </div>

                </section>

            </div>
        </main>

        <!-- FOOTER -->
        <footer class="dashboard-footer">
            <div class="scrolling-announcements">
                <div class="scrolling-text"></div>
            </div>
            <div class="scrolling-credits">
                <div class="scrolling-text-credits">
                    EMEĞİ GEÇENLER: Nahit Furkan ÖZNAMLI • Barış KÖSE • Muhammed Mert SAYAN
                </div>
            </div>
        </footer>
    </div>


    <!-- SCRIPTS -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Sunucudan gelen video dosya yolu listesi
        window.videoFilePaths = @Html.Raw(videoFilePathsJson);
        window.defaultVideoFilePath = @Html.Raw(defaultVideoFilePath != null ? $"\"{defaultVideoFilePath}\"" : "null");
    </script>

    <script>
        async function loadWeatherToday() {
            try {
                const res = await fetch('/Home/Weather7');
                const data = await res.json();
                const box = document.getElementById('weatherToday');
                if (!data.length) {
                    box.innerHTML = '--°C';
                    return;
                }
                box.innerHTML = `
                    <img src="${data[0].iconUrl}" style="width:50px">
                    <span>${data[0].tempC}°C</span>
                `;
            } catch { }
        }
        loadWeatherToday();
    </script>

    <script src="~/js/dashboard.js"></script>
</body>
</html>
